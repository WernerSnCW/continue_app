{
  "name": "continue-app",
  "version": "0.0.1",
  "private": true,
  "description": "Continue? — a Souls-like death tally tracker.",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "pnpm --filter @continue/mobile dev",
    "build": "pnpm --filter @continue/mobile build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "check": "pnpm -r typecheck && pnpm -r lint && pnpm -r test"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
